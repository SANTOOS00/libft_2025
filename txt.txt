⚙️ كيفاش كتخدم الدالة:

كتتحقق واش s ماشي NULL.
→ إذا كانت NULL، ترجع NULL.

كتشوف واش start أكبر من طول s.
→ إذا نعم، يعني البداية خارج النص، فترجع سلسلة فارغة "".

كتحدد شحال من حرف خاصنا ننسخ فعلاً.
→ إذا كان len أكبر من الباقي من s، ننسخ غير اللي متبقي.

كتحجز الذاكرة (malloc) بعدد الأحرف اللي غادي ننسخ + 1 (لـ '\0').

كتنسخ الحروف من s[start] حتى العدد المحدد.

كتضيف '\0' فالنهاية باش تكون سلسلة صالحة.⚙️ كيفاش كتخدم الدالة:

كتتحقق واش s ماشي NULL.
→ إذا كانت NULL، ترجع NULL.

كتشوف واش start أكبر من طول s.
→ إذا نعم، يعني البداية خارج النص، فترجع سلسلة فارغة "".

كتحدد شحال من حرف خاصنا ننسخ فعلاً.
→ إذا كان len أكبر من الباقي من s، ننسخ غير اللي متبقي.

كتحجز الذاكرة (malloc) بعدد الأحرف اللي غادي ننسخ + 1 (لـ '\0').

كتنسخ الحروف من s[start] حتى العدد المحدد.

كتضيف '\0' فالنهاية باش تكون سلسلة صالحة.char	*ft_strtrim(const char *s1, const char *set)




valgrind ./a.out --leak-check=full